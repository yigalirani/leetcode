
    def alienOrder(self, words: List[str]) -> str:
      order=defaultdict(set)
      words_hash={}
      def update_is_bigger(a,b):# is bigger than b
         order[a].add(b)
      for word in words:
         for i in range(1,len(word)):
            sub=word[0:i]
            if (words_hash.get(sub))
              

         
         

        